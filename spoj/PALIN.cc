#include <bits/stdc++.h>
#define max_string_length

using namespace std;

void do_work () {
	fgets(str, max_string_length,


int main () {
	ios_base :: sync_with_stdio(false);
	cin.tie(NULL);
	int n;
	cin >> n;
	for (int i = 0; i < n; i ++) do_work();

